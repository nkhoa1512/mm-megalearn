{
  "name": "mm-megalearn",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "vite preview",
    "verify": "esbuild scripts/verify-role-level-model.jsx --bundle --platform=node --format=esm --loader:.jsx=jsx --jsx=automatic --packages=external --log-level=warning --outfile=node_modules/.cache/verify-role-level-model.mjs && node node_modules/.cache/verify-role-level-model.mjs",
    "check:tables": "esbuild scripts/check-table-widths.jsx --bundle --platform=node --format=esm --loader:.jsx=jsx --jsx=automatic --packages=external --log-level=warning --outfile=node_modules/.cache/check-table-widths.mjs && node node_modules/.cache/check-table-widths.mjs"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.22.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.1.0"
  }
}
